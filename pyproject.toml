[project]
name = "starmap-svg"
version = "0.2.0"
description = "Generate SVG starmaps for selected coordinates and time."
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
dependencies = [
    "svgwrite>=1.4",
]

[tool.uv]
package = false
